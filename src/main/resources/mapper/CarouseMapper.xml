<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ltd.newbee.mall.dao.CarouselMapper">

    <resultMap id="CarouselResult" type="ltd.newbee.mall.entity.Carousel">
        <result property="carouselId" column="carousel_id" jdbcType="INTEGER"/>
        <result property="carouselUrl" column="carousel_url" jdbcType="VARCHAR"/>
        <result property="redirectUrl" column="redirect_url" jdbcType="VARCHAR"/>
        <result property="carouselRank" column="carousel_rank" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="VARCHAR"/>
        <result property="createUser" column="create_user" jdbcType="INTEGER"/>
        <result property="updateTime" column="update_time" jdbcType="VARCHAR"/>
        <result property="updateUser" column="update_user" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        carousel_id, carousel_url, redirect_url, carousel_rank, is_deleted,
        create_time, create_user, update_time, update_user
    </sql>

    <select id="getCarouselPage" parameterType="Map" resultMap="CarouselResult">
        select
        <include refid="Base_Column_List"/>
        from
        tb_newbee_mall_carousel
        where
        is_deleted = 0
        order by
        carousel_rank
        desc
        <if test="page != null and limit != null">
            limit #{page}, #{limit}
        </if>
    </select>

    <select id="getTotalCarousels" resultType="int">
        select count(*)
        from tb_newbee_mall_carousel
        where is_deleted = 0
    </select>

    <update id="deleteBatch" parameterType="arraylist">
        update
        tb_newbee_mall_carousel
        set
        is_deleted = 1, update_time = now()
        where
        carousel_id
        in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="updateByPrimaryKey" parameterType="ltd.newbee.mall.entity.Carousel">
        update
            tb_newbee_mall_carousel
        set carousel_url  = #{carouselUrl, jdbcType=VARCHAR},
            redirect_url  = #{redirectUrl, jdbcType=VARCHAR},
            carousel_rank = #{carouselRank, jdbcType=INTEGER},
            is_deleted    = #{isDeleted, jdbcType=TINYINT},
            create_time   = #{createTime, jdbcType=TIMESTAMP},
            create_user   = #{createUser, jdbcType=INTEGER},
            update_time   = #{updateTime, jdbcType=TIMESTAMP},
            update_user   = #{updateUser, jdbcType=INTEGER}
        where carousel_id = #{carouselId, jdbcType=INTEGER}
    </update>

    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="CarouselResult">
        select
        <include refid="Base_Column_List"/>
        from tb_newbee_mall_admin_user
        where admin_user_id = #{adminUserId, jdbcType=INTEGER}
    </select>

</mapper>
